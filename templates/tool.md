# （例: Docker）

> ツールの正式名称を書く。略称だけでは識別できない場合は正式名称を使う。
>
> **見出しは、この型の言葉をそのまま写さない。** 型が示すのは各節の役目であり、見出しの文言ではない。
> 中身を予測できる具体的な言葉へ置き換える（標準の `C-02`）。
>
> | 型の見出し | 置き換えた例 |
> |---|---|
> | 何が変わるか | 何が速くなり、何が速くならないか |
> | 落とし穴 | 日本語で必要になる2つの設定 |
> | 使いどころと、使わない場面 | 採用してよい条件 |

| 項目 | 内容 |
|---|---|
| 発信日 | （例: 2026-08-31） |
| 作成者 | （例: 山田 太郎（基盤チーム）） |
| 機密区分 | （例: 社内限） |
| 保守責任者 | （例: 基盤チーム） |
| 最終確認日 | （例: 2026-08-31） |
| 対象の版 | （例: Docker 28.4） |
| 想定読者（宛先） | （例: このツールを初めて使うバックエンド開発者） |
| この文書を読んだあと読者ができること | （例: 手元でコンテナを起動し、停止できる） |

## 結論

> **この文書が答える問いを1文で書き、その答えを先に書く。** 読者は最初から順に読まない。
> 理由は3つまでに絞る。詳しい根拠は下の節へ回す。

（例: 答える問いは、いま使っている X から Y へ移るかである。移る準備は始めるが、いま全面的に置き換えない。）

## 何をするツールか

> 1文で書く。どの問題を解くのかを含める。機能の一覧は書かない。

（例: Docker は、アプリケーションとその実行環境をひとまとめにして動かすツールである。）

## 何が変わるか

> 導入する前と後を並べる。機能の名前ではなく、読者の手元で変わることを書く。数値が取れるなら数値で書く。

| 場面 | 導入前 | 導入後 |
|---|---|---|
| （例: 新しい開発者が環境を作る） | （例: 手順書を見て半日かかる） | （例: 命令1つで15分で終わる） |
| （例: 動かないという報告が来る） | （例: 環境差の切り分けに時間を取られる） | （例: 同じ像で再現するため切り分けが要らない） |

## 使いどころと、使わない場面

> 役に立つ場面と、役に立たない場面を両方書く。片方だけでは読者が判断できない。

| 判断 | 場面 |
|---|---|
| 役に立つ | （例: 開発者が3人以上いて、環境の差で困っている） |
| 役に立つ | （例: 動かす環境を、手順書ではなくファイルで残したい） |
| 向かない | （例: 1人で1台だけで開発している。得られるものより手間が上回る） |
| 向かない | （例: 対象が GUI アプリケーションで、画面の描画が要る） |

## なぜこのツールを使うのか

> 比べた候補と、選んだ理由を書く。決定の記録が別にあるなら、その参照先を書く。
>
> **採否がまだ決まっていないツールでは、この表を使わない。** 決めていない判断を「不採用」と書くと事実に反する。
> 代わりに「いつ採用してよいか」の条件を箇条書きで示す。

| 候補 | 採否 | 理由 |
|---|---|---|
| （例: Docker） | （例: 採用） | （例: 開発者全員の環境で同じ版が動くため） |
| （例: 手元への直接インストール） | （例: 不採用） | （例: 環境差の切り分けに時間がかかったため） |

## 使う前に要るもの

> 版数まで書く。「入っていること」を確かめる命令も書く。

| 要るもの | 用途 | 確認方法 |
|---|---|---|
| （例: Docker 28.4 以上） | （例: コンテナの実行） | （例: `docker --version` が版を表示する） |

## 使い始める手順

> 開始条件と、成功したときの見え方を書く。手順は10ステップ以内にする。

**開始条件**

- [ ] （例: Docker Desktop が起動している）

**手順**

1. （例: `docker build -t sample .` を実行する）
2. （例: `docker run --rm sample` を実行する）

**成功したとき**: （例: 標準出力に `hello` と表示される。）

## よく使う操作

> 目的から命令を引けるようにする。網羅はしない。

| したいこと | 命令 |
|---|---|
| （例: 動いているコンテナを見る） | （例: `docker ps`） |
| （例: 止まったコンテナを消す） | （例: `docker container prune`） |

## 落とし穴

> 公式文書を読んでも分からないこと、実際に間違えたことを書く。ここが最も価値の高い節である。

- （例: `docker compose up` は `.env` を読む。ファイルが無いと環境変数が空のまま起動し、原因が分かりにくい失敗になる）

## 扱わないこと

| 扱わないこと | 理由 | 参照先 |
|---|---|---|
| （例: 全オプションの一覧） | （例: 提供元の文書が正本であるため） | （例: 公式リファレンス） |

## 関連文書

| 文書名 | 役割 | 参照先 |
|---|---|---|
| （例: ツール一覧） | （例: 他のツールの文書を探す） | （例: `docs/index.md`） |
